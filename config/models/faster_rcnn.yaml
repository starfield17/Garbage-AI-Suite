# config/models/faster_rcnn.yaml
# Faster R-CNN 模型配置

model_path: "models/faster_rcnn/best.pth"
confidence_threshold: 0.5
device: "cpu"

# 类别映射（可选，覆盖全局映射）
class_mapping:
  1: "Kitchen_waste"      # COCO 从 1 开始
  2: "Recyclable_waste"
  3: "Hazardous_waste"
  4: "Other_waste"
