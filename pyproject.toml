[project]
name = "garbage-ai"
version = "2.0.0"
description = "AI-based garbage detection and classification system"
requires-python = ">=3.10"

[tool.uv.workspace]
members = ["shared"]

[tool.ruff]
line-length = 100
target-version = "py310"
